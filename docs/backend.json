
{
  "entities": {
    "Issue": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Issue",
      "type": "object",
      "description": "Represents an IT issue reported by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the issue."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who reported the issue. (Relationship: User 1:N Issue)"
        },
        "title": {
          "type": "string",
          "description": "Title of the issue."
        },
        "issueType": {
          "type": "string",
          "description": "The general category of the issue."
        },
        "customIssueType": {
            "type": "string",
            "description": "A user-defined issue type, if 'Other' is selected."
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the issue."
        },
        "anydesk": {
            "type": "string",
            "description": "AnyDesk address for remote support.",
            "pattern": "^\\d*$"
        },
        "status": {
          "type": "string",
          "description": "Current status of the issue (e.g., Open, In Progress, Resolved).",
          "format": "string"
        },
        "priority": {
          "type": "string",
          "description": "Priority level of the issue (e.g., High, Medium, Low).",
          "format": "string"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the issue was reported.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the issue was last updated.",
          "format": "date-time"
        },
        "attachments": {
          "type": "array",
          "description": "Links to attachments related to the issue.",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "id",
        "userId",
        "title",
        "issueType",
        "description",
        "status",
        "priority",
        "createdAt",
        "updatedAt"
      ]
    },
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the IssueTrackr application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "displayName": {
          "type": "string",
          "description": "Display name of the user."
        },
        "role": {
          "type": "string",
          "description": "Role of the user (e.g., User, Admin).",
          "format": "string"
        },
        "phoneNumber": {
          "type": "string",
          "description": "Phone number of the user.",
          "pattern": "^\\d{11}$"
        }
      },
      "required": [
        "id",
        "email",
        "displayName",
        "role"
      ]
    },
    "ChatMessage": {
      "title": "Chat Message",
      "type": "object",
      "description": "Represents a single message within a ticket's chat session.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The ID of the user who sent the message."
        },
        "displayName": {
            "type": "string",
            "description": "The display name of the user who sent the message."
        },
        "text": {
          "type": "string",
          "description": "The text content of the message. Optional if audio is present."
        },
        "audioUrl": {
          "type": "string",
          "description": "The URL to the audio file for a voice note. Optional if text is present."
        },
        "imageUrl": {
          "type": "string",
          "description": "The URL to an image attachment. Optional."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the message was sent.",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "displayName",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles. Listing of users is disabled to prevent unauthorized access. The 'userId' parameter represents the unique identifier for a user.",
          "params": [
            {
              "name": "userId",
              "description": "Unique identifier for the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/issues/{issueId}",
        "definition": {
          "entityName": "Issue",
          "schema": {
            "$ref": "#/backend/entities/Issue"
          },
          "description": "Stores issues reported by a specific user. The 'userId' parameter represents the owner of the issue, ensuring path-based ownership. The `issueId` parameter represents the unique identifier for an issue within a user's issues.",
          "params": [
            {
              "name": "userId",
              "description": "Unique identifier for the user who owns the issue."
            },
            {
              "name": "issueId",
              "description": "Unique identifier for the issue."
            }
          ]
        }
      },
       {
        "path": "/users/{userId}/issues/{issueId}/messages/{messageId}",
        "definition": {
            "entityName": "ChatMessage",
            "schema": {
                "$ref": "#/backend/entities/ChatMessage"
            },
            "description": "Stores chat messages for a specific issue. Both the user and support staff can post messages here.",
            "params": [
                {
                    "name": "userId",
                    "description": "Unique identifier for the user who owns the issue."
                },
                {
                    "name": "issueId",
                    "description": "Unique identifier for the issue."
                },
                {
                    "name": "messageId",
                    "description": "Unique identifier for the chat message."
                }
            ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to ensure Authorization Independence, DBAC, and support secure list operations (QAPs). It leverages structural segregation and consistent access modeling for clarity and maintainability.\n\n*   **Users Collection:** Stores user profiles. Listing users is disabled to prevent unauthorized data access.\n*   **User-Owned Issues:** Issues are stored in a subcollection `/users/{userId}/issues/{issueId}`. This establishes clear ownership and simplifies security rules. This approach guarantees that listing issues is always scoped to a specific user.\n*   **Issue-Owned Messages:** Chat messages are stored in a subcollection under each issue, ensuring they are tightly coupled to the ticket they belong to.\n\nThis structure avoids `get()` calls in security rules by directly associating issues with their owners, ensuring Authorization Independence. QAPs are satisfied through path-based ownership, allowing secure and efficient data retrieval for each user."
  }
}
