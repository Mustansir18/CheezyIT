
{
  "entities": {
    "Issue": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Issue",
      "type": "object",
      "description": "Represents an IT issue reported by a user.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "Reference to the User who reported the issue. (Relationship: User 1:N Issue)"
        },
        "ticketId": {
          "type": "string",
          "description": "A user-friendly, sequential ticket identifier, e.g., TKT-000001."
        },
        "title": {
          "type": "string",
          "description": "Title of the issue."
        },
        "issueType": {
          "type": "string",
          "description": "The general category of the issue."
        },
        "customIssueType": {
            "type": "string",
            "description": "A user-defined issue type, if 'Other' is selected."
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the issue."
        },
        "anydesk": {
            "type": "string",
            "description": "AnyDesk address for remote support.",
            "pattern": "^\\d*$"
        },
        "status": {
          "type": "string",
          "description": "Current status of the issue.",
          "enum": ["Open", "In-Progress", "Resolved", "Closed"]
        },
        "assignedTo": {
            "type": "string",
            "description": "The UID of the user this ticket is assigned to."
        },
        "assignedToDisplayName": {
            "type": "string",
            "description": "The display name of the user this ticket is assigned to."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the issue was reported.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp indicating when the issue was last updated.",
          "format": "date-time"
        },
        "completedAt": {
          "type": "string",
          "description": "Timestamp indicating when the issue was resolved.",
          "format": "date-time"
        },
        "resolvedBy": {
            "type": "string",
            "description": "The UID of the admin or IT support user who resolved the ticket."
        },
        "resolvedByDisplayName": {
            "type": "string",
            "description": "The display name of the user who resolved the ticket."
        },
        "unreadByAdmin": {
            "type": "boolean",
            "description": "True if there are unread messages for an admin or IT support."
        },
        "unreadByUser": {
            "type": "boolean",
            "description": "True if there are unread messages for the ticket owner."
        },
        "region": {
            "type": "string",
            "description": "The region this ticket originated from, inherited from the user who created it."
        }
      },
      "required": [
        "userId",
        "ticketId",
        "title",
        "issueType",
        "description",
        "status",
        "createdAt",
        "updatedAt",
        "region"
      ]
    },
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the IssueTrackr application. Roles define access: 'User'/'Branch' roles can only manage their own tickets. 'it-support' and 'Admin' can manage tickets across multiple users based on their assigned regions. The 'Admin' role has more efficient data fetching capabilities for reports and listings compared to 'it-support'.",
      "properties": {
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "displayName": {
          "type": "string",
          "description": "Display name of the user."
        },
        "role": {
          "type": "string",
          "description": "Role of the user, defining their access level. Can be User, Branch, it-support, or Admin.",
          "enum": [
            "User",
            "Branch",
            "it-support",
            "Admin"
          ]
        },
        "phoneNumber": {
          "type": "string",
          "description": "Phone number of the user.",
          "pattern": "^\\d{11}$"
        },
        "region": {
            "type": "string",
            "description": "The geographical region of a standard user."
        },
        "regions": {
            "type": "array",
            "description": "For Admin/Support roles, a list of assigned regions. Can include 'all'.",
            "items": {
                "type": "string"
            }
        }
      },
      "required": [
        "email",
        "displayName",
        "role"
      ]
    },
    "ChatMessage": {
      "title": "Chat Message",
      "type": "object",
      "description": "Represents a single message within a ticket's chat session.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The ID of the user who sent the message."
        },
        "displayName": {
            "type": "string",
            "description": "The display name of the user who sent the message."
        },
        "text": {
          "type": "string",
          "description": "The text content of the message. Optional."
        },
        "audioUrl": {
            "type": "string",
            "description": "URL to the audio file in Firebase Storage. Optional."
        },
        "link": {
            "type": "string",
            "description": "A URL for a call or other resource. Optional."
        },
        "type": {
            "type": "string",
            "description": "The type of message (e.g., standard user message or a system notification).",
            "enum": ["user", "call_request"]
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the message was sent.",
          "format": "date-time"
        },
        "isRead": {
            "type": "boolean",
            "description": "True if the message has been read by the recipient."
        }
      },
      "required": [
        "userId",
        "displayName",
        "createdAt"
      ]
    },
    "Announcement": {
      "title": "Announcement",
      "type": "object",
      "description": "Represents a single master announcement sent by an admin. This serves as a central log.",
      "properties": {
        "title": { "type": "string", "description": "The title of the announcement." },
        "message": { "type": "string", "description": "The message content of the announcement." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp indicating when the announcement was created." },
        "startDate": { "type": "string", "format": "date-time", "description": "The start date and time for the announcement to be visible. Optional." },
        "endDate": { "type": "string", "format": "date-time", "description": "The end date and time after which the announcement will be hidden. Optional." },
        "createdByUid": { "type": "string", "description": "The UID of the admin who sent the announcement." },
        "createdByDisplayName": { "type": "string", "description": "The display name of the admin who sent the announcement." },
        "target": {
          "type": "object",
          "description": "The targeting criteria for the announcement.",
          "properties": {
            "roles": { "type": "array", "items": { "type": "string" } },
            "regions": { "type": "array", "items": { "type": "string" } },
            "users": { "type": "array", "items": { "type": "string" } }
          }
        },
        "recipientCount": { "type": "number", "description": "The number of users the announcement was sent to." },
        "recipientUids": {
            "type": "array",
            "description": "The UIDs of all users who received the announcement.",
            "items": { "type": "string" }
        },
        "readBy": {
            "type": "array",
            "description": "The UIDs of all users who have read the announcement.",
            "items": { "type": "string" }
        }
      },
      "required": ["title", "message", "createdAt", "createdByUid", "createdByDisplayName", "target", "recipientCount", "recipientUids"]
    },
    "UserNotification": {
      "title": "User Notification",
      "type": "object",
      "description": "Represents a single notification or announcement sent to a user.",
      "properties": {
        "title": { "type": "string", "description": "The title of the notification." },
        "message": { "type": "string", "description": "The message content of the notification." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp indicating when the notification was created." },
        "startDate": { "type": "string", "format": "date-time", "description": "The start date and time for the announcement to be visible. Optional." },
        "endDate": { "type": "string", "format": "date-time", "description": "The end date and time after which the announcement will be hidden. Optional." },
        "isRead": { "type": "boolean", "description": "True if the user has read the notification." },
        "announcementId": { "type": "string", "description": "A reference to the master announcement document, if applicable." },
        "createdByDisplayName": { "type": "string", "description": "The display name of the user who sent the announcement." }
      },
      "required": ["title", "message", "createdAt", "isRead", "createdByDisplayName"]
    },
    "TicketCounter": {
        "title": "Ticket Counter",
        "type": "object",
        "description": "Stores the current count for generating sequential ticket IDs.",
        "properties": {
            "count": {
                "type": "number",
                "description": "The last used ticket number."
            }
        },
        "required": ["count"]
    },
    "RegionSettings": {
        "title": "Region Settings",
        "type": "object",
        "description": "Stores the list of available regions in the application.",
        "properties": {
            "list": {
                "type": "array",
                "description": "An array of region names.",
                "items": { "type": "string" }
            }
        }
    },
    "RoleSettings": {
        "title": "Role Settings",
        "type": "object",
        "description": "Stores the list of assignable user roles.",
        "properties": {
            "list": {
                "type": "array",
                "description": "An array of role names.",
                "items": { "type": "string" }
            }
        }
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/backend/entities/User"
      },
      "description": "Stores user profiles. Listing of users is disabled to prevent unauthorized access. The 'userId' parameter represents the unique identifier for a user."
    },
    "/users/{userId}/issues/{issueId}": {
      "schema": {
        "$ref": "#/backend/entities/Issue"
      },
      "description": "Stores issues reported by a specific user. The 'userId' parameter represents the owner of the issue, ensuring path-based ownership. The `issueId` parameter represents the unique identifier for an issue within a user's issues."
    },
    "/users/{userId}/issues/{issueId}/messages/{messageId}": {
      "schema": {
        "$ref": "#/backend/entities/ChatMessage"
      },
      "description": "Stores chat messages for a specific issue. Both the user and support staff can post messages here."
    },
    "/users/{userId}/notifications/{notificationId}": {
      "schema": {
        "$ref": "#/backend/entities/UserNotification"
      },
      "description": "Stores notifications and announcements for a specific user."
    },
    "/announcements/{announcementId}": {
      "schema": {
        "$ref": "#/backend/entities/Announcement"
      },
      "description": "Stores a central log of all announcements sent. Only accessible by Admin/Root users."
    },
    "/system_settings/{settingId}": {
      "schema": {
        "oneOf": [
            { "$ref": "#/backend/entities/RegionSettings" },
            { "$ref": "#/backend/entities/RoleSettings" },
            { "$ref": "#/backend/entities/TicketCounter" }
        ]
      },
      "description": "Stores global system settings like available regions, roles, and counters. Only accessible by Root users."
    }
  }
}
