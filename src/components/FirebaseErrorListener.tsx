
'use client';

// This component is intentionally left blank as it was causing application crashes.
// The error handling is now managed locally within the useDoc and useCollection hooks.
export function FirebaseErrorListener() {
  return null;
}
